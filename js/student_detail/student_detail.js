// scripts.js

// This function can be extended to handle dynamic content or interactions
document.addEventListener("DOMContentLoaded", function () {
    // Example: Show an alert when the back button is clicked
    document.querySelector('.back-btn').addEventListener('click', function(event) {
        alert("Going back to the students list.");
    });
});
